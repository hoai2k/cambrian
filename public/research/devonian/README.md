# Deployed copy — generated

Written by `tools/research/viewer-bundle.mjs` (`npm run devonian:viewer`) so the reference viewer ships
with the site at `<site>/research/devonian/`. Do not edit anything here: the page is
`tools/research/viewer/index.html`, the data is `docs/research/devonian/viewer/`, and the canonical images are the
full-resolution set in `docs/devonian/canonical/`, re-encoded here at 1400 px for the web.
