# Gemuendina candidate01 — actual Three.js playback gate

Root reviewed select portrait and all four Blender poses; no gross tearing was visible. Eyes
remain an explicit unresolved issue. Candidate01 is immutable; this does not approve publication.
Terra medium executes this established harness adaptation. CWD is the expansion-repo root.

Verify frozen-playback-01.sha256 and every input before execution; record the manifest digest.
Run exactly:

```sh
QA_BASE_URL=http://127.0.0.1:4181 node tools/devonian/creatures/gemuendina/rework-v3/review-playback-01.mjs
```

Use elevated Chrome startup when required. The root-owned Vite4181 must respond; do not stop
unowned servers. Do not use QUICK mode. Budget five minutes. Exclusive new outputs are under
../devonian-authoring/gemuendina/rework-v3/playback-01/. The harness refuses existing output,
intercepts candidate GLBs without changing public assets, invokes existing runtime clip/anchor
checks, captures every action at three phases, five inspection views, authored/default palette
comparisons and full action video, including the held terminal Death. It verifies candidate
hashes again before writing review.json. Expected console JSON: clips 18, audit passed, errors [].

After success verify frozen inputs again; return report, video and PNG paths, source/input/report
hashes. Save a separate execution-state alongside the output directory. Root will inspect actual
images/playback. Do not declare visual quality from a structural pass. On any input mismatch,
request/JS/audit/Chrome failure, existing-output refusal or missing evidence, preserve results and
return exact error. No source/public/Git fixes, image retouching, parameter reductions or retries
into an existing output. Other author works on a NEW candidate only; never alter candidate01.
