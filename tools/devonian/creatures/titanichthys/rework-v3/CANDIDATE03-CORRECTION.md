# Candidate03 — measured decimation defect and bounded correction

The focused diagnostic completed successfully as a measurement. Its original frozen input manifest was independently reverified after execution. `result.json` SHA-256 is `05ac5a359b241c2838e05f912e183bfa43dd8ff9298c14f4dc4c1151636bcfb0`; actual NPZ is `67623b7af8b8f786fafeea4446885788c4c0660387a7a7996fd80d472c931047`.

The 362,152 dense body corners had filtered linear R in [0.0304892231, 0.1585968733]. After the exact 0.26 decimator, 114,340 corners had R in [−0.0420058705, 0.3117181659]. Twenty negative-red corners represent only four vertices, IDs8–11, on the right head/oral junction: x0.74655–0.83425, y−2.18170–−2.04123, z−0.05567–−0.04714. Ten corners belong to exterior material0 and ten to oral material2. They repeat the same invalid per-vertex RGB despite different oral/exterior UV islands. G/B remain finite and positive; seven alpha values exceed1 by1.192e−7. There are no nonfinite values. This establishes colour extrapolation across the collapse operation, including substantial red error; it does not support labelling all errors as rounding.

No clipping is used to repair these values. Candidate01, candidate02 partial output, diagnostic and frozen original sources stay unchanged. Candidate03 replaces the colour-assignment order:

1. Keep neutral-white Color on export copies through the same decimation ratios. Discard its post-collapse values; record them as diagnostic evidence only.
2. Sample fins and eyes from the exact accepted atlas at each final UV using the existing independently checked decoder, bilinear sampling and explicit sRGB-to-linear conversion. This avoids interpolating physical pigment through collapses.
3. For the body, retain the original dense area-weighted filter, same semantic region, normal compatibility, .055 exterior radius and .028 oral radius. Sample that saved continuous field on the closest dense source triangle **within the final polygon's exact material role**. Use nonnegative triangle weights summing to1. This preserves the designed low-frequency field rather than replacing it with sparse raw atlas samples. Alpha is authored as1. Body colour cannot escape its source triangle or role's channel range.

The closest-point helper tests the triangle's plane interior and closed edges. Restricting an edge coordinate to its closed segment is a geometric projection, not albedo clipping. Degenerate triangles reduce to valid edges/vertices. A per-role BVH selects the source triangle; source/body coordinates are unchanged. Per-role projection distance percentiles/maxima, coefficient ranges and source/target RGB extrema are reported for review. Actual runtime BVH behavior has not been executed by Astra.

The neutral body decimation must exactly match the diagnostic's23,513 positions,114,340 loop vertex indices, corner UVs and per-loop material IDs. Any difference stops execution. Original accepted full geometry/key hashes and body semantic/gape correspondence also remain mandatory. This prevents a pigment repair from silently changing the measured LOD shape or palette boundaries. Final visual review must still assess colour projection near the mouth.

The existing exporter correspondence/quantization utility is retained unchanged; it transfers final source Color into existing GLB COLOR_0 bytes and proves all non-colour bytes are untouched. It receives valid assigned pigment. The structural checker is byte-identical to02 except its new output directory: no threshold or assertion is relaxed. Rig,18 actions, weights, five nested anchors, full PBR maps, texture dimensions, LOD ratios and roughness choices are unchanged.

Source-only validation: four new Python sources parse. Six exact closest-triangle fixtures,3,000 seeded arbitrary-scale triangle/normal-projection cases and two degenerate cases pass; maximum coefficient error6.614e−11, with convex pigment bounds verified. This is not a Blender export or visual pass. The21 existing frozen camera/action/LOD views are retargeted to03 without creative changes. Runtime playback, final actual-mesh eye volume and visual orbital/oral/general gates remain outstanding. The44MB raw full-export size is unresolved; final packaging must satisfy the established delivery budget, with no silent reduction authorized here.
