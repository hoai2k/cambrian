# Candidate05 — measured skinned eye coordinate-space correction

Candidate04's colour stages passed; G1 failed during correspondence before the GLB colour-transfer buffer was written. All22 partial files/log and original sources remain unchanged. The focused two-eye diagnostic exited0. Result SHA-256: fc923e3942809d6b38d4476fe02b003a8474de045b110e69af012f577a9e1c92. Actual source/query NPZ SHA-256: 6772c4782aa38061e2bd3589334bd2775bdd636911d05a1b074e15bde832987a. Every102 frozen diagnostic input entry was independently reverified after execution.

Each eye contains644 exported vertices and2702 source corners. Old raw-local mapping misses every644 rounded-position bucket and every original2e−6 coordinate/UV bound per eye, with worst five-coordinate error2.0713899136. Both measured world-coordinate strategies match every vertex: zero missing rounded buckets, zero matches above2e−6 and zero ambiguities above5e−5. Maximum residual2.9802322388e−8 is UV precision. Actual eye colour errors are7.5509319366e−6 left and7.6158931606e−6 right, both below half a normalized16-bit step (.5/65535≈7.6295109483e−6).

This is a coordinate-space omission, not an observed rounded-bucket boundary defect. Eye mesh coordinates are local spheres; skinned glTF POSITION has the object's matrix_world applied. Body and fins were authored in world-aligned mesh coordinates. `atlas_pigment_03.py` now follows the installed exporter's exact operation: mesh float32 positions multiplied by matrix_world, stored as float32, then converted to glTF Y-up. It requires mesh users to be skinned, preventing accidental use of that convention for an unsupported unskinned mesh.

All round6 lookup,2e−6 position/UV correspondence,5e−5 ambiguity checks, valid pigment/quantization gates and unchanged-non-colour-byte assertion remain unchanged. No adjacent-bucket search, tolerance increase or albedo clipping was introduced. The transfer report adds maximum coordinate/UV error for each primitive.

`candidate_05.py` differs from04 only by version/output paths and the corrected atlas helper. Checker05 and renderer05 are retargeted copies: all18 actions must exist and match full/LOD channels, sample values, timing and interpolation. Oral Attack/Bite/Heavy/Eat/Ability checks remain on both. The23 views include actual imported LOD Attack and Eat actions. Geometry, rig, animation trajectories, anchors, weights, material maps, texture dimensions, LOD ratios and post-decimation convex body pigment method are unchanged.

Source validation:4 Python scripts parse. A pure-Python replay executed the new helper on actual measured source-eye corners and actual failed-export eye accessors in a temporary subset fixture. All1288 exported eye vertices matched; quantization remained within half a16-bit step; the helper proved non-colour bytes unchanged. Evidence is candidate05-source-validation.json. No Blender or production rerun occurred in Astra. Existing04 output and all earlier candidates stay preserved. G1/G2/G3, final runtime/eye/oral/general/visual review and packaging still must pass.
