# Candidate07 — fixed screen rectangle included legitimate lower jaw

G1 and G2 passed. G3-01 stopped at full Ability.5 after14 records: all16 unrounded sections at this phase had zero crossings, but38/340 fixed screen rays hit underside. The failed result/log and candidate07 are preserved. G4-01 was not run. This evidence does not justify another geometry derivative08.

Independent read-only CPU arithmetic (no Blender job) reconstructed actual glTF transforms in float64 and selected the original aperture's192 vertices from the exported mesh. Source-formula selection error was at most1.20e-7. It reproduced the recorded actual triangle hit positions within1.10e-6 for06 and9.54e-7 for07. Candidate06 had46 underside hits inside the projected geometric aperture; candidate07 had only1, with37 below/outside the opening.

The remaining07 pixel(967,565), triangle112663, is only0.000323 above the geometric rim. Its three bind vertices include an exact rim vertex at index163 and two adjacent first-collar vertices only0.0005059/0.0005090 from rim samples163/164. This is the continuous, rounded first lip collar, which legitimately carries underside material. It is not a separated panel or crossing floor. A fixed rectangle that once contained an interior defect also covers the outside of a changed mouth silhouette.

The decisive independent check uses the actual inner lip's material interface, not a hand-sized exclusion band. The connected oral-material submesh has exactly one closed boundary:196 vertices full,101 LOD. These are obtained by exact identical-position welding of glTF corner splits, with no spatial epsilon. All410 full/226 LOD boundary corner duplicates have exactly identical joint/weight maps in06 and07. There is no ambiguous boundary selection or hidden cap.

Classifying the recorded full rays against this actual posed boundary gives06:46 interior underside hits/34 exterior;07:0 interior/38 exterior. The closest07 excluded pixel is still0.000072965 outside the inner-lip boundary. The difference from the geometric aperture correctly accounts for the first lip collar. Evidence is saved in candidate07-pixel-classification-01.json, candidate07-boundary-classification-02.json and candidate07-boundary-topology-validation.json; the two independent arithmetic sources are preserved.

The corrected checker02 preserves the original section algorithm,16 planes,58 sampled poses and all numeric thresholds exactly. It still requires zero strict underside/oral crossings. It retains all340-ray role counts and every underside hit; only the failure predicate is corrected to test underside inside the actual posed oral-material boundary. A non-simple/multiple boundary or unexpected projection stops for review. Both original06 full/LOD must reproduce actual interior hits and section crossings before07 is checked, so the correction cannot merely disable the failed condition.

Only G3-02 and then G4-02 should run, against the same07 files. No re-export, source-blend save, new geometry candidate, G1/G2 repetition, image processing or eye changes are required. The five actual imported-GLB render views remain unchanged apart from their new result directory and sweep dependency. Actual remaining full/LOD sweep and final mouth art approval are still pending. Root separately owns eye seating and normal-texture studies.
