# Triassic 3D work status

This is the current coordination ledger for unfinished Triassic creature work. An agent must claim
a row here before editing its model or shared pipeline, commit that claim to `main`, and change the
row to **finished** in the same commit that delivers the verified work. A claimed row must not be
duplicated by another agent. Optional creature regenerations are deliberately outside the active
scope until the required repairs and roster-wide finishing pass are complete.

Updated: 20 September 2026.

| ID | State | Owner | Work | Completion evidence |
| --- | --- | --- | --- | --- |
| T3D-01 | **finished** | `/root/askeptosaurus_regen` | Redraw Askeptosaurus in a straight neutral modeling pose with the tail near two-thirds of total length; generate a new textured Tripo body; preserve the current generated body as viewer-selectable **Backup Model**; build the authored/puppet pair with matching rig, clips, anchors, LODs and portraits; ship it and clear its preview status. | Canonical and Tripo replacement delivered; 33 joints, 24 clips and 3 anchors; exact authored/twin/LOD parity; animated preserved-pose **Backup Model**; 61-phase throat and skin checks, real viewer playback, portraits, typecheck, build, Triassic suite and asset checks passed. See `tools/triassic/creatures/askeptosaurus/README.md`. |
| T3D-02 | **finished — audit and priority repairs** | `/root/throat_audit` | Audit actual shipped Triassic actions and restore the shared palate/floor helper; repair Hupehsuchus' short mouth and posterior attachment, Henodus' severed upper fringe and Ceratites' deforming shell. Shared-kit/ShoreKit delivery is T3D-08/T3D-09; the remaining six custom builders are explicitly pending T3D-12. | SHA-bound current sweep covers 54 models, 1,264 clip/model combinations and 31,600 poses. Priority creature audits and visual evidence pass. `throat-repairs/final-audit-summary.json` enumerates the six remaining mixed-lining species; roster-wide recovery is not complete. |
| T3D-02a | **finished** | `/root/throat_audit` | Keep the complete Ceratites coil rigid and retire invented crown mouth geometry. | `82786a19`: anatomical shell checks on both variants across every clip; zero contaminated vertices/deformation; paired audit, refreshed portraits, typecheck and build pass. |
| T3D-02b | **finished** | `/root/throat_audit` | Extend Hupehsuchus' jaw to the full lip and keep the posterior jaw attached to its pouch. | Paired audit passes; 162 authored/67 puppet shared-rim vertices stay attached at all 61 phases of 23 clips (zero gap); portraits refreshed. |
| T3D-02c | **finished** | `/root/throat_audit` | Preserve Henodus' hanging upper fringe during jaw opening and publish correct Grab loop metadata. | 80 authored/18 puppet tooth faces retained on skull; upper fringe intact in side/below views; paired audit and 61-phase/all-clip posterior attachment check pass, zero gap; Grab loops. |
| T3D-03 | **finished** | `/root/roster_finish` | Audit the roster-wide rest-pose and animation contract: neutral/base pose, mouth closure, dynamic attack/dash motion, anchors, paired-body parity and missing required clips. Land small deterministic repairs, including `Grab` where absent, and record larger findings without starting optional regeneration. | Generated matrix now reports 26 of 26 packaged bodies contract-met: identity-rest jaws, required anchors/clips, `Attack`/`Sprint`, skin attributes, and decoded authored/puppet/LOD parity are present. Visual throat review remains T3D-02's separately completed gate. |
| T3D-04 | **finished** | `/root/roster_finish` | Port proven centreline and limb-root blending corrections into the shared Triassic pipeline without changing already-correct output. | `appendage_vertex_mask` and `appendage_excluded_centreline` now preserve the four-proven two-pass correction as opt-in shared helpers; Python compilation and the generated packaged roster audit pass with no rebuilt asset change. |
| T3D-05 | **finished** | `/root/roster_finish` | Make the shipped `Grab` clips loop for Henodus, Keichousaurus, Nothosaurus and Placodus, then rebuild the authored/puppet/LOD triplets and refresh paired audits. | Keichousaurus, Nothosaurus and Placodus rebuilt with looping `Grab`; Henodus landed in `2e599809` with its paired audit and posterior-jaw check. The generated matrix now reports no missing `Grab` loop. |
| T3D-06 | **finished** | `/root/roster_finish` | Add Shonisaurus' required `Grab` and `Breath` clips, then create its first shared paired audit and review the new action playback. | Packaged GLBs now contain 21 contract clips with a 1.2-second looping `Grab`; neutral exporter scale/root channels are removed during package; the shared decoded paired audit proves exact rig, clips and anchors. |
| T3D-07 | **finished** | `/root/roster_finish` | Render and publish the three actually missing Triassic portrait sets without changing creature bodies: Cartorhynchus, Cymbospondylus and Mixosaurus. | All authored select/card/thumb/studio renders and procedural-twin portraits are published and visually reviewed; `node tools/triassic/publish-portraits.mjs --check` reports all 26 shipped bodies current. |
| T3D-08 | **finished** | `/root/roster_finish` | Port the current builders other than T3D-02's Ceratites, Henodus and Hupehsuchus to the restored separate palate/floor helper, preserving all later skinning and animation repairs. | All eleven builders and authored/puppet/LOD triplets were rebuilt in T3D-08A–F; decoded paired audits pass and direct packaged topology checks prove separate closed rigid palate/floor shells. |
| T3D-08A | **finished** | `/root/roster_finish` | Port the separate-palate/floor measured-room call into the current Birgeria and Archelon builders. | Both authored/puppet/LOD triplets were rebuilt on current `main`; their packaged decoded audits pass with exact rig and animation parity, and the builders report separate oral lining geometry. |
| T3D-08B | **finished** | `/root/roster_finish` | Port the separate-palate/floor measured-room call into the current Atopodentatus and Cartorhynchus builders. | Both authored/puppet/LOD triplets were rebuilt on current `main`; their packaged decoded audits pass with exact rig and animation parity, and the builders report valid capped lining geometry. |
| T3D-08C | **finished** | `/root/roster_finish` | Port the separate-palate/floor measured-room call into the current Cymbospondylus and Mixosaurus builders. | Both authored/puppet/LOD triplets were rebuilt on current `main`; their packaged decoded audits pass with exact rig and animation parity, and the builders report separate oral lining geometry. |
| T3D-08D | **finished** | `/root/roster_finish` | Port the separate-palate/floor measured-room call into the current Aphaneramma and Mystriosuchus builders. | Both authored/puppet/LOD triplets were rebuilt on current `main`; packaged decoded audits pass with exact rig and animation parity, and direct topology audits prove separate closed rigid palate/floor shells. |
| T3D-08E | **finished** | `/root/roster_finish` | Port the separate-palate/floor measured-room call into the current Mosasaurus and Odontochelys builders. | Both authored/puppet/LOD triplets were rebuilt on current `main`; packaged decoded audits pass with exact rig and animation parity, and direct topology audits prove separate closed rigid palate/floor shells. |
| T3D-08F | **finished** | `/root/roster_finish` | Port the separate-palate/floor measured-room call into the current Rhaeticosaurus builder. | The authored/puppet/LOD triplet was rebuilt on current `main`; its packaged decoded audit passes with exact rig and animation parity, and the direct topology audit proves separate closed rigid palate/floor shells. |
| T3D-09 | **finished** | `/root/throat_audit` | Port Coelophysis, Macrocnemus and Tanystropheus from the separate `shorekit.oral_lining` implementation to the same rigid palate/floor contract. This is a cross-kit change and must retain their runner-specific action and skinning repairs. | All three authored/puppet/LOD shell topology checks and paired action audits pass. Coelophysis/Macrocnemus posterior seams stay exactly attached across every clip; selected strict gape views have zero through-body pixels. Fresh portraits and SHA-bound 166-clip/4,150-pose diagnostic evidence are recorded in `throat-repairs/shorekit.md`. |
| T3D-09a | **finished** | `/root/throat_audit` | Convert Tanystropheus to separate rigid palate/floor shells. | Packaged authored/puppet/LOD closed-shell checks pass; paired 28-clip audit passes; Bite/SnapRight strict culling review has zero through-body pixels; portraits refreshed. |
| T3D-09b | **finished** | `/root/throat_audit` | Convert Coelophysis to separate shells and attach its posterior jaw during hard snaps. | All 29 clips pass; 106 authored/69 puppet shared-rim vertices keep zero gap at 61 phases per clip; strict snap through-hole count 2,367→0; portraits refreshed. The cap selector is restricted to the head plane; accidental remote fan strands are removed and a packaged rest-edge guard prevents recurrence. |
| T3D-09c | **finished** | `/root/throat_audit` | Convert Macrocnemus to separate shells, retain the palate inside the skull and attach the posterior mandible. | Packaged 26-clip paired audit passes; 68 authored/46 puppet shared-rim vertices keep zero gap at 61 phases per clip; strict Bite/Snatch culling has zero through-body pixels; portraits refreshed. |
| T3D-10 | **finished** | `/root` | Review the seven newly delivered plant/prop canonicals, redraw inaccurate or poorly framed subjects, generate a Tripo source for each approved subject, process each into the instanced-prop contract, integrate it into the Triassic scenery and specimen viewer, and document the provenance. | Seven canonical-reviewed Tripo sources and sanitized task records preserved; seven reduced, base-pivoted, vertex-pigment GLBs visually reviewed and shipped. Runtime densities/mappings, generated footprint colliders, specimen entries, 13-asset manifest, 143 collider checks, prop validation, typecheck and build pass. |
| T3D-11 | **finished** | `/root` | Keep delivered canonical subjects out of the generated undecided review list after model delivery. | `apply-selections.mjs --dry-run` reports mutually exclusive delivered, greenlit, rework and undecided counts; delivered subjects no longer appear in the undecided section. |
| T3D-10A | **finished** | `/root/prop_canonical_audit` | Produce corrected canonicals and clean 2048-square Tripo inputs for Neocalamites, Coenothyris cluster, Daonella bed, Cidaris and Encrinus litter; derive ground-free inputs for the approved Bjuvia and Pleuromeia designs. | Five candidate03 canonicals and seven `model-inputs/<id>/input.png` deliveries visually reviewed; root approved the final contact sheet for Tripo. Complete silhouettes, no substrate slabs, corrected shell/test/columnal anatomy. Native outputs, 11 exact edit prompts, byte-identical originals, hashes and resampling disclosure are in `canonical/prop-inputs-2026-09-19/`. Viewer refresh, seven-input packaging validation, typecheck and build pass. No Tripo submitted. |
| T3D-12 | **split** | — | Recover the six custom oral builders still shipping mixed jaw/skull sacs. Split into T3D-12A and T3D-12B below so two bounded batches can run in parallel worktrees. | See the two batch rows. |
| T3D-12A | **finished** | `session fvrvz3` (claude/3d-modelling-animation-review-fvrvz3), worktree `worktree-agent-ae1e0b849fa3f0378` | Ported Hybodus, Saurichthys and Placodus from their custom mixed sacs to the separate rigid palate/floor contract (`T.oral_shells`, imported into the two self-contained builders), keeping the 15 September repairs and every later fix. The two fish arrived gaping, so the shared helper now takes a `(palate, floor)` seam and room pair and a floor front; each shell is built about its own jaw's edge of the lumen on the mouth's measured lateral axis, every shell vertex is seated inside the head by rays, the skull's hinge section is capped, the mouth's own rim is folded, Hybodus' inward-wound mandible is voted right, and Saurichthys' opercular seams are sealed. Placodus' hinge sphere is gone (cut caps, textured, replace it). Record: `docs/triassic/throat-repairs/README.md`, plates beside it. | `oral-shell-audit.mjs` passes on authored/puppet/LOD for all three; `throat-audit.mjs` reports 0 mixed vertices on all three; paired audits exact; `idle-bones` every joint owns skin; `skin-tears` unchanged (5.93x / 3.61x / 12.36x); strict-cull gape seen-through px before → after: Placodus 0/0/0 → 10/10/0 (pass), Saurichthys 373/372/0 → 1/1/2 (pass), Hybodus 498/667/222 → 346/517/6 — `Bite`, the mouth's own verdict, passes, and the 343/497 px left at `Heavy`/`Attack` coincide with the shipped body's own opercular slit behind the mouth corner (traced pixel by pixel: one back-facing skin surface, no front face, 0.06 of a body behind the hinge), a generation defect recorded rather than modelled over; portraits refreshed; asset sizes refreshed; typecheck, build and `npm run triassic` pass. |
| T3D-12B | **finished** | `session fvrvz3` (claude/3d-modelling-animation-review-fvrvz3) | Decide per animal, then act: Dinocephalosaurus (expected to need no oral geometry at all — a closed head behind a painted lip), Keichousaurus (mixed sac → separate shells, seam kept on the pale-zone lip line), Phragmoteuthis (remove the invented crown opening and every beak/peristome surface; anchors only, as Ceratites now is). Record the per-animal verdict (palate / floor / neither) with the measurement behind it. No regeneration. | Verdicts measured and recorded in [`throat-repairs/oral-verdicts.md`](throat-repairs/oral-verdicts.md): **Dinocephalosaurus neither** (sac removed; strict-cull gape 0–3 px through the head at five peak-gape shots without it, 98–113 px without the hinge tissue too, so the sac closed nothing); **Keichousaurus palate + floor** (`T.oral_shells` at 0.84 of the measured room, seam unchanged, 0 px through the body, oral figure 6.57× → 2.34×, Grab loop intact); **Phragmoteuthis neither** (peristome cut, sewn lining and both mandibles retired, crown closed, 0 px, `gape-crown.py` moot and says so). All three authored/puppet/LOD triplets rebuilt with exact decoded parity; `oral-shell-audit.mjs` reports separate shells (Keichousaurus) or no lining (the other two); `throat-audit.mjs` lists none of the three as mixed-lining; skin figures unchanged (7.00×, 2.34×, 5.37×); every joint owns skin; portraits refreshed and checked; typecheck, build and `npm run triassic` pass. Hybodus, Saurichthys and Placodus remain T3D-12A's. |
| T3D-13 | **finished** | `session fvrvz3` (claude/3d-modelling-animation-review-fvrvz3) | Build a **mouth editor** in the specimen viewer (`&mode=mouth`): a human aims a mouth cut on a Tripo-derived body — how far back the cut goes (moves the hinge fore/aft), where the mouth sits along and across the head, and the angle of the mouth line and cut plane. Export names the exact file (sha256) the way `region.ts` does, for both exact builder use and guidance. Pure module + `npm run mouth` check + browser driver + `docs/viewer-mouth.md`, following sculpt/mark/stretch. | `src/viewer/mouth/` (pure `mouth.ts`, `MouthEditor.tsx`, `store.ts`), three handles on the orbit view with the mandible side lit; `npm run mouth` 107 checks (guess from jaw/socket/section, basis, test, handles, flip, file and refusals); `node tools/mouth-browser.mjs` on Placodus: seated on the jaw bone (2,961 mandible vertices), a handle found by hover and dragged then undone, fields and Square, export with the page-measured sha256 equal to the file on disk, `npm run triassic:mouth` accepting it and refusing a tampered copy, kept through view mode, forgotten on reload, then the original pose framed from its box and exported as `appliesTo: generation`; `docs/viewer-mouth.md`; Mouth row on the `?debug` index; typecheck, build, `npm run mark`, `npm run stretch`, `npm run ancientseas` pass. |
| T3D-14 | **finished** | `session fvrvz3` (claude/3d-modelling-animation-review-fvrvz3) | Shonisaurus: move the eyes slightly forward and up with the socket following, and confirm it carries no oral geometry. Nothosaurus: rotate the head pose to face straight forward in the builder (no runtime patch) so its mouth cut aligns; keep its skin at or under 2.98x. | **Shonisaurus**: globes seated at the painted eye (+2.1/+4.1 % of head length forward, +5.3/+5.2 % up, centre 0.0022 raw inside the skin, 77/81 % of the globe surface under it, was 67/59 %), measured and bounded in `build.py`, recorded in `validation.json` (`eyes`); palate, floor, throat tube and tooth rows removed and `package-audit.mjs` refuses anything the runtime classifier matches; strict-cull gape 4 → 6 px (tolerance 12); skin 1.44x, rig/clips/sockets unchanged, paired audit exact. **Nothosaurus**: head unbent in the mesh before binding (yaw −20.35° → −0.29°, Dinocephalosaurus' carry, `neckUnbending.applied`, original pose published by `base-poses.mjs`); jaw cut is a plane fitted to the modelled lip on both flanks (old seam 0.0027 raw under the lip, 0.0048 on the left; new ±0.0009 per flank); palate/floor kept rigid, hinge tissue now two rigid halves seated by ray parity, all one hidden `Nothosaurus oral lining` (`oral-shell-audit.mjs` passes; `throat-audit.mjs` 0 mixed, was 146); strict-cull gape 53 → 1 px; skin 2.98x unchanged (main's `Walk` gait re-applied on top, 2.99x against 3.10x on main's file); every joint owns skin; Swim/Sprint steady head untouched. Before/after renders in `docs/triassic/throat-repairs/`, verdict rows in its README. Rebuilt triplets, paired audits, portraits, asset sizes, typecheck, build, `npm run triassic`. |
| T3D-15 | **finished** | `session fvrvz3` (claude/3d-modelling-animation-review-fvrvz3), worktree `worktree-agent-a77498d98acabd79b` | Roster-wide jaw-to-head skinning: every jaw's swing in Bite/Attack/Heavy/Eat must blend smoothly into skull and neck rather than tear or shear at the connection. Promote the `lag.mjs` diagnostic (skin travel round a joint over the joint's own travel) into `tools/triassic/` and run it with `skin-tears.mjs` over every delivered body; repair each failing jaw in its builder. | The fault was one class: every jawed body's mandible is a separate shell weighted `jaw` at 1 against a skull-weighted body, so the two copies of each cut-rim vertex parted by the gape — Hybodus 6.05 % of a body, Saurichthys 3.51, Nothosaurus 2.19, Cartorhynchus 2.02, Mystriosuchus 1.80, Placodus 1.72, Aphaneramma 1.65, Mixosaurus 1.64, Cymbospondylus 1.14, Dinocephalosaurus 0.67, Keichousaurus 0.54 (eleven past the 0.5 % threshold), and invisible to `skin-tears.mjs` because no edge crosses a seam between shells. `tools/triassic/lag.mjs` (in `npm run triassic`) measures the cut's opening over every clip beside the per-joint lag/follows figures, `skin-tears.mjs` gained `--all`, `--json` and a per-bone `jaw`/`skull` line, `tools/triassic/jaw-views.py` renders the junction at a posed frame. `T.jaw_junction` in `_pipeline/tripo.py` (via `shorekit` for the shore kit) is one weight field over body and shell, asserted equal at every rim pair, the throat following the jaw under the hinge and the shell ramping to full jaw over 0.015 of a body; nineteen builders ported and rebuilt as full triplets with exact paired audits. After: every cut at 0.00–0.03 % except corner points (Nothosaurus 0.28, Keichousaurus 0.33); no body's skin figure above its pre-repair figure (Saurichthys, Aphaneramma and Atopodentatus each needed one correction — tooth-root rim, limb term, `throat=.6` — and came back to exactly 3.61x / 4.43x / 3.90x); every joint owns skin; oral shells untouched (`oral-shell-audit.mjs` passes); `hidden-parts.mjs --check` passes with Placodus' rename kept through its rebuild; Walk/Flop re-applied to Nothosaurus, Hybodus and Saurichthys; portraits re-rendered and current; asset sizes refreshed. Not repaired, recorded: Hybodus' opercular crack, Placodus' gastral-armour rim (1.61 % in Crawl, a second shell, not a jaw), Helicoprion's 14.33x, the corner-of-mouth points. Table, renders and the four wrong rim classifiers tried: [`jaw-skinning.md`](jaw-skinning.md), `throat-repairs/<id>-jaw-before/after.png`. Typecheck, build, `npm run triassic`. |
| T3D-16 | **finished** | `session fvrvz3` (claude/3d-modelling-animation-review-fvrvz3) | Verify the seven Tripo-derived props (T3D-10) are actually in the game and fix what is not. | Found and fixed: the viewer catalogued them under ids without the `triassic-` prefix the manifest gives them (the Triassic gate had been red since 867ebd4), the catalogue check demanded a preview reason from a final prop, and — the real gap — `src/render/sea.ts`'s `floraSets` never had the seven kinds, so the simulation scattered and collided against them while the renderer drew nothing and fetched nothing. Now drawn with scale-1 fallbacks that swap in the GLBs; the table is total over `FloraKind` so the compiler refuses the next such kind. A Triassic browser drive fetches all seven meshes (200) with no console errors across nursery, shelf, reef, pavement and boulders; the viewer loads each prop's own mesh unbadged; `npm run triassic` 535 checks, `npm run props` 143, `npm run sim:gate` pass. |
| T3D-17 | **finished** | `session fvrvz3` (claude/3d-modelling-animation-review-fvrvz3), worktree `worktree-agent-a5255f4077b906c40` | Roster verification sweep against the owner's 15 September viewer defect list and the four measurable mouth/cut/dash/attack checks, on the shipped GLBs after T3D-12/14: each item re-checked with a render or a number, small fixes landed, larger ones recorded as rows. | [`roster-verification-2026-09-20.md`](roster-verification-2026-09-20.md), one table per part, per animal, renders under `verification/`. Of the owner's twelve: Keichousaurus' wave travels head→tail (+3 phases per tail joint) and its fore paddles push backward while extended; Aphaneramma's, Mystriosuchus' and Tanystropheus' feet all travel with their joints (lag 0.98–1.13); Archelon's shell is rigid (0.21 % RMS worst clip); Helicoprion is shut in Idle/Guard (1.15°), Eat opens to 58.8° and closes to 0; Shonisaurus' globes sit on the painted eye (0.4–0.6 radii off, 79/86 % under the skin); Nothosaurus' head is straight (−0.24°); Askeptosaurus' tail is 0.632 against the canonical's 0.62–0.63 and its backup is on the Model control; all 27 portrait sets published and current; Ceratites' attacks reach forward (+7.8 % L after a −2.4 % rear-back; Heavy is its Withdraw); Cartorhynchus' front blades deform 2.2 / 3.2 % RMS with the right one blended into the chest (T3D-18). Strict-cull gape over every opening clip of every jawed body at its measured peak: 19 pass, Cartorhynchus 51 px (T3D-18), Cymbospondylus 15, Henodus 66, Placodus 19 and Shonisaurus 22 (T3D-19), Hybodus on its recorded opercular slit. Every hidden mesh listed: all oral fill except Placodus' `Palate crushing teeth`, **fixed** by renaming it `Upper crushing teeth` and rebuilding the triplet byte-identical but for the name, with `tools/triassic/hidden-parts.mjs --check` now in `npm run triassic` refusing the old file. Skin table re-swept (Helicoprion 14.33× in `Flop`, Coelophysis 7.74× in the snaps). New rows T3D-18…T3D-23. Typecheck, build, `npm run triassic`. |

| T3D-18 | **finished** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-a8684615cfdfaf70e` | Cartorhynchus: the mouth's corner leaks at full gape and the front blades are the roster's softest. `gape-solid.py` at the measured peak of `Ability`/`Attack`/`Bite`/`Eat` sees 29 / 30 / 47 / 51 px of backdrop through the body at the commissure (tolerance 12; `Heavy` 0, `Grab` 1) — T3D-08B's separate shells and caps leave a sliver at the corner. The fore blades' per-bone rigid-fit residuals at `Sprint` are 3.1 / 3.7 / 4.2 % of L on the left and 3.2 / 3.9 / 5.2 % on the right (whole chain 2.21 / 3.18 %, the highest fore limb in the roster; next Odontochelys 1.79 %); the right chain carries 18 % more weight and blends 232 of it into `chest` against 60 on the left, so the two blades are pulled by the trunk unequally although the rest shape is mirror-symmetric to 0.5 % L. Close the corner (cap or flange at the commissure) and re-measure both blades' weights on their own bone chains. | **Mouth**: the 29/30/47/51 px were measured on the file *before* T3D-15's rebuild (`ad20c13`, 06:25; the junction landed 08:17). Re-shot on that file they reproduce (25/30/47/51) and a ray through them meets no front face and first the inside of the far cheek at the hinge station — the slot under the corner of the mouth `T.jaw_junction` closes (2.02 % of L here). On the shipped file: **0 px** on all six opening clips, before and after this rebuild; nothing added at the commissure. **Blades**: the cause was the roots — `thin_clusters`' one-vertex seat picked opposite corners of the two blades and `T.seat`'s depth walk stopped at different depths, so the right chain started in the chest with a radius 0.102 against 0.081. Roots are now the base band in the trunk's section metric carried to 0.30 of the section (`E_BAND`/`E_SEAT`, recorded as `limbSeats`): base bands mirror 0.038 L. `Sprint` rigid residual L/R chain **2.36 / 2.46 %** (per bone 1.25/1.23, 0.74/0.82, 0.33/0.34; `Swim` 1.91/1.99), trunk blend 70.6 / 78.9 weight in the builder's record (12 % apart; `chest` still 13 vs 29 because the right base is 0.024 L further forward, which is the pose). Skin **3.72× → 2.98×**. Paired audit exact parity (paddles-over-tail 1.54 / 1.11), `oral-shell-audit`, `throat-audit`, `idle-bones`, `lag`, `hidden-parts --check`, `skin-tears` clean; portraits re-rendered and published. Renders under `verification/cartorhynchus-*-after-T3D-18.png` and `-before-T3D-15.png`; rows in `roster-verification-2026-09-20.md` A2, B1, B3 and the skin table.
| T3D-19 | **partly done** | **Henodus done by T3D-32B** (its pixels were never the fringe: a ray through every one of them meets one back face of the mandible and nothing else, and capping the cut took 407 as drawn to 0); Cymbospondylus, Shonisaurus and Placodus unclaimed | Three bodies a few pixels over the strict-cull tolerance, each at an edge rather than a hole: Cymbospondylus `Heavy` 15 px (`Attack` 13, `Eat` 10) as slivers at the hinge corner; Henodus 35–66 px on every opening clip along the hanging upper denticle fringe's own one-sided tooth edges (the fringe T3D-02c kept on the skull); Placodus `Eat` 19 px on the mandible's front cap at a grazing corner (T3D-12A recorded 10 on `CrushBite`/`Bite` and did not shoot `Eat`; the rebuild here is byte-identical) — **traced and recorded by T3D-22's session**: on the T3D-15 file `Eat` 10 / `Attack` 13 / `CrushBite` 13 / `Ability` 11 / `Bite` 6 / `Heavy` 0 / `Pry` 0, and a ray through every failing pixel of the three worst meets no front face on its line — 7–8 px are the mandible's one-polygon lip flange at the front corner of the mouth seen edge-on from its back, 5–6 px the skull's front-cut cap at the same corner seen from behind, every neighbour a pixel over either honest gape or a front face of the teeth, floor or jaw: a silhouette sliver, not a hole, and the skin is double-sided in play (marked renders `verification/placodus-*-gape-opened-after.png`, the seven shots in `tools/triassic/creatures/placodus/gape-solid.json`); Shonisaurus `Attack` 22 px at its measured peak (phase 0.25; T3D-14 shot 0.5 and saw 0) along the upper tooth row at the commissure, the tooth-crown sliver class T3D-14 recorded. Trace each failing pixel by ray before touching geometry: a fringe tooth that is a single sheet is the generation's and may be a recorded defect rather than a repair. | A ray-traced account per body of what each failing pixel is; ≤ 12 px or a recorded generation defect; marked renders in `verification/`. |
| T3D-20 | **partly done** | **Macrocnemus done by T3D-32B** (the step detector was reading the right flank's countershading; fitted to the painted lip read with the matched filter, 0.03043 raw → 0.00398); Tanystropheus by T3D-32C; Coelophysis unclaimed | The shore kit cuts the mouth flat at the median of per-station albedo readings that disagree with each other: Coelophysis' readings run 0.15–0.78 of the section height over 12 stations and the cut departs from its own measured line by 0.0617 raw (6.3 % of L) at the worst station; Macrocnemus 0.0304 (3.1 %); Tanystropheus 0.0116 (0.9 %). The marine kit fits a curve (Aphaneramma 0.0010, Mystriosuchus 0.0007) or a plane through the slit on both flanks (Nothosaurus ±0.0009). Read which feature the albedo method actually found per station on these three heads (CLAUDE.md's three cases), then fit rather than take a median. Jaw *weights* are T3D-15's; this is the cut. | Cut-to-measured-line deviation ≤ 0.005 raw on all three, the feature named; strict-cull gape still ≤ 12; paired audits. |
| T3D-21 | **claimed** | Helicoprion done; Saurichthys by T3D-32C | Fin roots seated off their skin. Saurichthys' right pectoral chain sits away from the fin it drives: `pec_tip_R` is 3.58 % of L from the nearest skin vertex at all (`pec_tip_L` 1.67 %), `pec_mid_R` 2.32 % (L 1.11 %), and the right chain dominates 126 vertices to the left's 247 — one fin is skinned to half the vertices of its mirror. Helicoprion's `Flop` (the shore gait `gaits.mjs` added) tears the skin 14.33× at `pec_upper_R` (405 edges past 2×; `Parry` 11.68× was the previous worst), which is the pectoral root's radius rather than the clip. Re-measure both pectoral chains on the mesh (`K.measure_radii`) and seat the joints on the fin. | Left/right pectoral chains within 20 % of each other in owned vertices and joint-to-skin distance; Helicoprion skin under its previous 11.68×; `idle-bones`, paired audits. **Helicoprion done** (`build.py` `pectoral` in its `validation.json`): each pectoral is the largest blade-thin patch under the flank, flooded over the mesh's own edges, stationed in 8 bands of geodesic distance from its tip (the two tips agree to 0.001; the geodesic *seat* they were stationed from before sat 0.037 apart), the two centrelines mirror to 0.0058 raw and are averaged, the root is the base station pulled 0.036 into the trunk by ray parity to a 0.020 margin, and the mid and tip joints sit on the centreline at 0.42 / 0.78 of its arc. The blade is weighted by its own 97th-percentile section per station (+0.018 ramp) and by arc length, with no `\|x\|` or thickness step, and the skin is relaxed (`T.relax_weights`, 3 passes) with every vertex forward of y −0.30 held so the jaw hinge and the whorl are byte-for-byte what the closing rotation was measured on. Parity off the shipped weights: owned area L 0.03096 / R 0.03099 (0.1 % apart); each chain dominates 99.8 % / 100 % of its own blade; blade joints' mean distance to the skin they dominate within 3.3 %; roots 0.0206 / 0.0210 inside the skin. Vertex counts 584 / 733 are 20.3 % apart because the right blade is meshed 20.3 % denser (646 thin vertices to 515 over the same box) — a count compares the tessellation, an area the fin. Skin: **14.33× → 4.21×** (`Eat`, on the held jaw), `Flop` 2.01× (was 14.33×), `Parry` off the list (was 11.68×), 6 of 22 clips over 2× (was 18); twin 10.55× unchanged (its jaw, held). `Flop` re-applied with `apply.mjs`, JSON by hand as before; `audit.mjs --package --decode` exact parity; `idle-bones` every joint owns skin; portraits republished; asset sizes. Saurichthys waits on T3D-15. |
| T3D-22 | **finished** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-a8684615cfdfaf70e` | Placodus' dash does not paddle: limb-root swept angle per `Sprint` cycle is 35° fore / 34° hind (`Swim` 24 / 23) where every other paddling reptile is 120–580° (Henodus 131, Dinocephalosaurus 138, Keichousaurus 219, Aphaneramma 539) and the tail carries the stroke (30.7 % of L at `tail_06`). "A limbed swimmer's dash has to paddle": decide whether a placodont is the exception the research supports or author the stroke, and record the swept angles in `validation.json` either way. | **Decided: the cruise is the research's tail scull and the burst paddles.** `docs/research/triassic-swimming.json` reads Placodus as a "negatively buoyant benthic bottom-walker with a slow tail scull" (Neenan: laterally flattened tail, short relatively unmodified limbs with probably webbed feet), so `Swim` keeps its trailing limbs at 24 / 23° swept; `Sprint` is the dash and the era rule holds — its stroke is authored over the unchanged tail wave (stretched forward to swept back along the flank, feathered recovery, hind pair a third of a beat behind, as the animal's own `Crawl` shoves) and sweeps **155.4° fore / 155.3° hind** per cycle by the builder's frame-to-frame sum, **154.4 / 154.7** re-measured off the packaged clip at 33 phases; foot lag 1.07 1.04 1.10 1.09. Tail amplitude untouched (`tail_06` yaw unchanged, the paired audit's tail-over-paddles bar still met). `validation.json` carries `limbSweepDegrees` for every clip and `locomotionDecision`. Skin: the root's arc is held inside what `Crawl`/`Pry` already ask, `Sprint` 9.42× → 11.33× on `fore_paddle_L`, the body's worst unchanged at 12.36× (`Pry`). Paired audit exact parity, `oral-shell-audit`, `throat-audit`, `idle-bones`, `lag`, `hidden-parts --check` clean; portraits are `Idle@0` of an unchanged mesh and rig and stay current. [Before](roster-verification-2026-09-20.md) B3 row and `verification/placodus-Sprint-before-T3D-22.jpg` / `-after-T3D-22.jpg`.
| T3D-23 | **partly claimed** | Atopodentatus by T3D-32A, Macrocnemus by T3D-32B, Dinocephalosaurus and Tanystropheus by T3D-32C, Aphaneramma/Rhaeticosaurus/Askeptosaurus by T3D-32D; Phragmoteuthis done; Helicoprion is the #37/#29 regeneration decision; Shonisaurus’ anchor unclaimed | Attack clips and anchors that do not use the animal the way the rule asks, measured on `anchor_attack_primary` over L: Atopodentatus `Heavy` pulls the head 33.8 % back before reaching 7.1 % forward; Aphaneramma `Heavy` −18.0 % / +11.5 % and `Ability` −24.9 % / +9.8 %; Rhaeticosaurus `Heavy` −14.4 % and never more than 1.0 % forward. Dinocephalosaurus' `NeckStrike`/`Attack`/`Heavy` and Tanystropheus' snaps are lateral sweeps of a neck already straight at rest (42 % lateral, 32–47 % back, ≤ 1 % forward) — decide whether a sideways sweep is the strike or a recoil. Phragmoteuthis' arms reach 3–4 % of L on every attack (Ceratites 14 %). Helicoprion's shut pose is the largest closing that keeps 95 % of the whorl under the palate (23.49°), which leaves the front of the mouth open with the whorl's crowns standing out below the snout in `Idle` and at the end of `Eat` (renders in `verification/`), and its `Bite` starts at the bind gape rather than shut — the whorl and the palate need a decision (fold the whorl's crowns, or cut the palate to take them) before the mouth can close. Askeptosaurus' `TailWhip` moves the skull anchor 0.9 % while the tail is the weapon (no tail anchor). Shonisaurus' `anchor_mouth_inside` is on `jaw`, alone in the roster. Macrocnemus' `hind_foot_L` skin travels 0.88 of its joint (every other foot 0.98–1.13). | Per item: a recorded decision or a re-authored clip/anchor with the same measurement re-run; no body's other figures worse. **Phragmoteuthis done** (`attackReach` in its `validation.json`, asserted in `build.py`; strips [before](verification/phragmoteuthis-attack-before.png) / [after](verification/phragmoteuthis-attack-after.png)): the tentacles lie straight along the axis at rest with their tips at the front of the body, so no rotation extends them, and the head's protraction is capped by the neck skin (0.36 tore it 5.77×), which is why the shipped reach was 3–4 %. The reach is now the **dart**: `body` carries the whole animal 0.10 L forward on a plateau envelope that rises after the gather and is held through the close, so the pair shuts out in front rather than back at rest; the funnel swings aft and the fins clamp with it; the tentacles' closing curl is 0.95 rather than 1.35 (a curl on a straight chain is a shortening). Attack anchor along the crown axis over L, packaged file, 41 phases: `Attack` **+13.6 % @0.55 / −2.4 % @0.13**, `Bite` +14.5 / −1.7, `Grab` +14.5 / −2.4, `Heavy` +14.4 / −3.3 (were +2.9–4.3 / −5.6–9.3); mean of all twelve tips +14.5 %. `Ability` is the ink blast, not an attack, untouched. Kept: twelve arms; the jet's translation channels (`Sprint` 0.2063, `Ability` 0.3458, the strike clips exactly 0); the closed crown (T3D-12B); `anchor_attack_primary` on `arm_04_06` with its chain. The paired audit's crown and anchor ratios now measure in the body's frame, since the dart moves every tip and socket alike. Skin 5.37× unchanged (`Guard`/funnel), twin 3.52× unchanged; `idle-bones` every joint owns skin; paired audit exact; portraits, asset sizes. The Helicoprion clause (whorl / shut pose / `Bite`'s open first frame) is **recorded, not acted on**: it is the #37/#29 regeneration decision. |
| T3D-24 | **finished** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-a688f8fc33e593367` | Askeptosaurus read as a stick. T3D-01 straightened the generation so its over-curved tail could be rigged, which was right, but nothing ever gave the shape back as **pose**: the bind pose is the straightened modelling pose, every portrait is shot at it, and `Idle` carried a tail amplitude of 0.008 rad (a quarter of a degree) of yaw with no vertical component anywhere. Author a resting curvature the swimming wave rides on, in the clips and never in the bind, **posed per clip** rather than stamped under all of them as one constant offset; raise the clip amplitudes and give the wave a dorsoventral component and a tip-growing lateral travel; make the portrait pose a builder-supplied option so Askeptosaurus can be shot from a frame that reads like its preserved backup while no other body's portraits change. | Done. `HOLD` in `build.py` names a **held shape per clip** as multipliers on one set of resting constants, and the wave rides on it: Idle the relaxed hold (trunk arch 0.114 rad, tail fall −0.348), Swim and Sprint straighter with the paddles laid back (0.074/−0.278 and 0.023/−0.209), Guard gathered over a humped shoulder (0.180/−0.452), Eat reaching down over the food (−0.076/−0.435), Grab braced (0.106/−0.400), the turns one long C through trunk, neck and tail (±0.468 rad of lean), Dive nose-down with the tail carried high (−0.104) and Rise its mirror (−0.522). The acts move *through* a shape rather than holding one — an attack gathers and then extends through the whole body, the tail strikes brace against their own swing, a hit recoils, a dash streaks, death goes slack — and `heldShape` in `validation.json` records the resolved shape at the start, middle and end of every clip, with the builder asserting the ten named holds are pairwise distinct and that each act's shape changes across it. The **phase step** was the lever the amplitude is not: twelve tail controls lagging 0.62 rad carry more than a whole wavelength and the tip contributions cancel (1.1 % of a body at .62 against a readable stroke at .40); `WAVE_STEP` .40, travel growing `.45+.55u^1.2`, `WAVE_VERT` .26 a quarter beat behind. Tail-tip travel measured back off the keyed actions with floors asserted: Idle 0.008 rad → 0.054 of a body travelled, Swim 0.074 → 0.188, Sprint 0.117 → 0.274. Bind pose untouched (`basePose.closedRestJaw` still true); skin 1.10x → **1.31x**, still the roster best (worst edge 3 instances on `body` in Sprint, 0.070→0.092); `lag.mjs` 0 pairs open past 0.2 %, worst 0.00 %; `idle-bones`, `oral-shell-audit`, `throat-audit`, `hidden-parts --check` clean; exact authored/twin/LOD rig, clip and anchor parity. The preserved backup is byte-for-byte unchanged (1.74x, 24 clips) because the held shapes and the raised amplitudes are gated off the `--backup` path. Portraits: `creature_render.run` takes `portrait_pose`, default `('Idle', 0)`, and Askeptosaurus names `('TurnLeft', .2)`; re-rendering Hupehsuchus with this pipeline and with `main`'s agrees to 5 px of CYCLES noise. Renders: [card before/after](verification/askeptosaurus-pose-card-before-after.png), [held shapes side](verification/askeptosaurus-pose-holds-side.png) / [top](verification/askeptosaurus-pose-holds-top.png), [Idle before](verification/askeptosaurus-pose-Idle-before.png) / [after](verification/askeptosaurus-pose-Idle-after.png), [Swim before](verification/askeptosaurus-pose-Swim-before.png) / [after](verification/askeptosaurus-pose-Swim-after.png). |
| T3D-25 | **finished** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-afe93f142072056cb` | Swap Askeptosaurus' two bodies: the preserved curved generation becomes the shipped `askeptosaurus.glb` and the 19 September straight regeneration becomes the backup. Each body keeps its own rigging and animation map; the promoted body gains a procedural twin and LOD, T3D-24's per-clip held shapes and real wave amplitudes re-tuned to its own chain, and the uncurling its hooked tail needs so `Heavy`, `TailWhip`, `Ability` and `Coil` reach full amplitude rather than the 28 % clamp it shipped with. | `FRONT` in `build.py` is the whole swap, checked by flipping it (with `FRONT=STRAIGHT` the straight body ships with its twin and the posed one becomes a single-model backup, each keeping its own map). The posed body's axis was rebuilt (the four paddles were in its geodesic bands, inflating it from the surface's own 1.578 to 2.025 and folding its first three tail controls back on each other at 68.9 and 89.9 degrees); excluding them gives 1.612 and a tail fraction of 0.606 against 0.562. The tail's 180.4 degrees of turn is opened in **pose only** -- 70 % carried into the bind by posing the rig and letting the skin follow its own weights, the rest per clip -- so no vertex moves by anything but the weights that already held it and the generation's surface, UVs and albedo are untouched. Skin **1.34x** (twin 1.27x, backup 1.31x), second on the roster; `lag.mjs` 0 rim points open, worst 0.00 %; `idle-bones`, `oral-shell-audit`, `throat-audit` and `hidden-parts --check` clean; exact full/twin rig, clip, anchor and inverse-bind parity with a byte-identical LOD1; zero throat gap at 61 phases of all 24 clips on all three variants. `posedExtentOverBind` is now measured and asserted per clip because the renderer sizes a body by its bind box and the uncarried build drew a swimming animal 1.38-1.56 times its own hitbox. `lag.mjs`' cut/lip axis now comes from the neck rather than the skin's longest extent, which a curled body breaks (26 of 27 shipped bodies unchanged). Renders: `docs/triassic/verification/askeptosaurus-swap-*.png`; record: `tools/triassic/creatures/askeptosaurus/README.md`. |

| T3D-26 | **finished** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-a3f86d7f922fe788f` | Askeptosaurus' front is off the trunk: `axis.neckTakeoffVsTrunkDegrees` 44.52 plus `axis.neckRestTurnDegrees` 34.13 put the head and neck at close to a right angle to the body in every clip, and the shoulder with the right pectoral on it goes round with them. `uncurl` removes a chain's internal curve but preserves its takeoff, and only the tail was ever carried into the bind. Bring the front into line by **distributing** the takeoff correction over the chest, the four cervicals and the skull rather than swinging it at the neck root (T3D-25 measured a rigid swing at 2.62x skin), carry it into the bind, assert a bound on the residual, and measure the right pectoral against its mirror. Surface, UVs, albedo, proportions, the tail's straightening and signed coil, and the `POSED`/`STRAIGHT`/`FRONT` split all stay. |Done. The diagnosis needed one correction before the fix: the head stood **67.7 degrees** off the trunk's own run, not 44.5 + 34.1 — the cervical chain's 34 degrees of internal turn is an S (+3.0 signed) and barely deflects the head, so almost the whole fault was takeoff, and the takeoff is 54.6 rather than 44.5 once it is measured against the trunk's **run** (hip to shoulder) instead of the `body`->`chest` chord, which on a trunk this curled is itself 17.1 degrees off it. `uncurl` now takes a cumulative `share` and applies that fraction of the takeoff arc at **each** joint of `NECKCHAIN` (chest, four cervicals, skull) — 9.1 degrees a joint, 17.2 at the worst once composed with the opening — so the front turns onto the trunk over its own length instead of being swung there at the root; equal per joint rather than per unit length, because weighting by segment length hands the skull 0.34 of the arc alone and measures a larger residual at the snout besides. The aim is carried **whole** into the bind (`fcarry`) and only the opening is left per clip: head against the trunk's run **67.7 -> 4.2 degrees at rest**, `Sprint` 0.5-5.2, `Idle` 6.5-8.1, the turns 3.3-15.1, `Coil` 27.8 because it pulls the neck round on purpose. `measure` records the range per clip and asserts it, `carry_rest` asserts no front joint exceeds 25 degrees, and the rest residual is asserted under 12 — so neither the right angle nor the rigid swing can return in silence. Skin **1.34x -> 1.37x** (twin 1.27 -> 1.30), against the 2.62x T3D-25 measured for the same correction swung at the neck root. **The right pectoral is rigged like its mirror and follows its own joint like its mirror** — new `paddle_audit` in `validation.json`: median skin-travel over joint-travel 1.07 against 1.16 (hind pair 1.12/1.14), roots 0.019 and 0.021 inside their own skin, distal blades owning 168 and 155 vertices; its cluster is genuinely the smaller (613 against 814) and sits 0.036 further forward, which is the generation's pose. It hangs off the shoulder, the shoulder went round with the head, and `chest`'s 9.1 degrees of the aim carries it. Two things had to follow the carried head: `carry_rest` hands back the per-bone map it bakes the skin through and the three anchors are placed through it (each one's distance to the nearest skin vertex unchanged to four decimals), and the posterior cut plane is no longer axis-aligned, so the builder records the plane it cut on and `auditCutAttachment` takes `{point, normal, tolerance}` as well as a coordinate — the other four bodies that use it are untouched. Surface, UVs, albedo, proportions, the tail's 0.70 carry and its signed coil all untouched; the straight regeneration's raw export is **byte-identical** (checked by running the previous builder beside the new one). `posedExtentOverBind` still in band (`Idle` 0.94-0.97, `Swim` 1.01-1.05, `Sprint` 1.03-1.06); `lag.mjs` 0 open past 0.2 % and all 27 bodies pass `--all`; `auditCutAttachment` 14 rim pairs at a worst gap of exactly 0 over 61 phases of 24 clips; `idle-bones`, `oral-shell-audit`, `throat-audit`, `hidden-parts --check` clean; LOD1 byte-identical to the twin; portraits re-rendered from `('TurnLeft', .2)` (kept — the old card's snout read as forked because the head was turned), `publish-portraits --check` and `update-asset-sizes` current; typecheck, `npm run build` and all **540** Triassic checks pass. One thing recorded rather than fixed: the shared mouth-camera guess has never found this animal's head (2.79 of an 8.72-unit body out, measured on the pre-fix file) — `docs/triassic/builder-requests.md`. Renders: [dorsal before](verification/askeptosaurus-front-holds-top-before.png) / [after](verification/askeptosaurus-front-holds-top-after.png), [both interleaved](verification/askeptosaurus-front-holds-top.png), [down the trunk's own run](verification/askeptosaurus-front-on.png), [the card](verification/askeptosaurus-front-card.png), [the full-range clips](verification/askeptosaurus-front-range.png). |

| T3D-27 | **finished** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-a41250ac7e5611491` | Build a **bend editor** in the specimen viewer (`&mode=bend`): the stretcher's shape with rotation in place of scale — two cuts, a bend axis and two angles (base and tip of the span), warping a raw generation live and holding a rigged body at rest as a measurement for its builder, with a **live angle readout that names the two references it is measured between** and offers the geometry reading and the bone-chain reading side by side. The answer to the diagnosis that went wrong three times on Askeptosaurus (T3D-25/T3D-26), where three defensible readings of "the trunk" disagreed by up to 43°. Pure module + panel + scene helpers + export hashed to the file + a consumer that re-measures + `npm run bend` + a browser harness + a debug-index row + `docs/viewer-bend.md`. |Done. **The span is two points on the animal** rather than the stretcher's coordinates-plus-tilts, and it had to be: Askeptosaurus' neck leaves its shoulder at **61° to its own long axis**, so cuts square to the frame's axis cover half the neck's length and a bend about them swings the head rather than bending the neck. Both cuts are square to the line between the two points, that line is the direction and the length, the base end is where the bend is anchored, and each end is where its own reading's trace starts. The two angles are turn **rates** across the span rather than offsets, so the rotation at the base cut is exactly identity however large they are and no number can kink the body where the span begins; the span keeps its own length through the bend (a rotation about one fixed pivot is a spiral, not a bend), behind the base cut nothing moves to the last decimal, and past the tip cut the far part is carried rigidly. **The readout is the tool.** No angle is shown without the two references it is between, each is said three ways (in the bend plane, out of it, in all — the out-of-plane figure *measured* by turning the base direction and seeing what is left, because projecting onto a plane can make an angle larger than it is in space and the obvious subtraction then reads a false zero), and every "after" is re-measured rather than predicted. The geometry reading **traces the body rather than binning a coordinate**: this animal's left paddle reaches further forward than its own snout, so an axial slab ahead of the neck averages the head with a flipper and reports the head running backwards. The trace is drawn on the body and reports its own residual — 0.009 ahead of the span, 0.062 behind it, i.e. excellent at the head and unusable at the curled limb-crossed trunk, which the panel says in orange instead of quoting a confident number. **The worked case, on the shipped body** (span `chest`→`skull`, 0.814 long, 9.4 % of the animal): against one tip chord, `body → chest` reads 2.8°, `tail_00 → chest` 14.3° and `chest → neck_00` 32.0° — **29.1° apart on one span, on one screen, each labelled**. Against the builder's own two references it reads 6.93° where `axis.carry.restHeadVsTrunkRunDegrees` records 4.17°, and the difference is *which point on the skull is the snout* (`anchor_attack_primary` 6.93°, `anchor_mouth` 9.03°, the builder's own measured landmark 4.17°) — explained, not reconciled, and the reason the export names the references it used. A 20° turn re-reads as 20° on the geometry (both windows lie outside the span) and spreads as 3.9/4.1/4.1/4.1/3.9° over `neck_00…skull`, which is the shape `uncurl` returns and `carry_rest` consumes. Where it sits: beside stretch, on a generation or a built body, never on the comparison twin or a prop; there is **no bake** in either direction. `tools/triassic/bend-check.ts` re-measures rather than reprinting and fails loudly on disagreement; it refuses a file whose hash or vertex count has moved. Checks: `npm run bend` (152), `npm run mouth`, `npm run mark`, `npm run stretch`, `npm run sculpt`, `npm run ancientseas` (587), `npm run triassic` (540), typecheck and build all pass. The deploy workflow ran only `npm run sculpt` of the viewer's editor checks, so the three that already existed and were not run on a push (`stretch`, `mouth`, `mark`) are wired in beside the new one. Browser: `node tools/bend-browser.mjs` drives the whole of it on Askeptosaurus. Renders: [the default span](verification/askeptosaurus-bend-opened.png), [the span on the neck](verification/askeptosaurus-bend-neck.png), the three trunk readings ([`body`](verification/askeptosaurus-bend-trunk-body.png), [`tail_00`](verification/askeptosaurus-bend-trunk-tail_00.png), [`chest`](verification/askeptosaurus-bend-trunk-chest.png)) and [a 20° turn](verification/askeptosaurus-bend-turned.png). |
| T3D-28 | **finished** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-a6194cd7b2b874b3f` | Make the specimen viewer's Animations pane **sticky across a change of creature**: the chosen clip, the position within it, the pause state and the *Base pose* selection carry over, with the *intent* kept apart from what is playing — a creature that lacks the chosen clip falls back to its resting clip without overwriting the intent, so a later creature that has it plays it again. Session-scoped like the viewer's other editor stores, never `localStorage`. The decision is a pure module with its own check wired into `package.json` and the deploy workflow, plus a real-browser harness. | `src/viewer/playback/selection.ts` decides what a loading body plays and is pure; `src/viewer/playback/store.ts` is the session copy (in memory, like the sculpt/mark/mouth/bend stores). `npm run playback` covers clip carried over, clip absent → resting clip with the intent retained, the third creature getting it back, the position clamped to a shorter clip but never written back onto the intent, the fall-back starting at zero, pause through all of it, a specimen with no clips, the base pose, and a model swap holding its own frame; wired into `.github/workflows/pages.yml` beside the other viewer checks. `node tools/playback-browser.mjs` drives it in Chromium against a preview build: Hallucigenia `Crawl` paused at 1.80/2.00 s → Anomalocaris (no `Crawl`: `Idle` at 0.00, still paused, `data-clip-intent` still `Crawl`, the pane saying so) → Wiwaxia `Crawl` back at 1.80 → its reduced model, same clip and frame → `Attack` 1.33 s clamped to Anomalocaris' 1.10 s and back to 1.33 → base pose sticky across a switch → reload forgets it. Five screenshots. Verified: `npx tsc --noEmit`, `npm run build`, `npm run playback`, `mouth`, `mark`, `stretch`, `sculpt`, `bend`, `ancientseas`, `triassic`. |
| T3D-29 | **finished** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-a5bcd46043f578c86` | The bend editor's owner feedback, three changes. (1) **An unbent head to aim on.** Bend mode holds a rigged body at rest and Askeptosaurus' rest already carries T3D-26's whole correction (`carry`: head 4.17° off the trunk's run against `headVsTrunkRunDegreesUncorrected` 67.68), so there was nothing left to aim. Reached by the route that already existed rather than a new artefact: a **rest-pose carry** joins the moves `base-poses.mjs` recognises — it is not a mesh unbending, so it carries no `applied` flag and is told by there being bones in `carriedBones` with some share of the aim in them — and Askeptosaurus now publishes `askeptosaurus.origpose.glb`. The panel says which body the numbers describe and warns, with the Model control's own wording, where that body's rest was moved before binding; `appliesTo` gains `origpose` beside `generation`, still `use: 'builder-measurement'` because the file is a published copy of `tripo-raw/`. (2) **The axle.** It was a roll about the span, so the handle swept the creature's long axis — a twist — while the body went ninety degrees the other way. It is now *derived* from the two planes and is not a handle at all; what a reviewer drags is a plane, and the plane's motion is the bend. An oblique aim's lean along the span is reported (`axis.twistDegrees`) rather than clamped. (3) **Two oriented planes** in place of the base/tip turn rates: each end's normal is the direction the creature's axis line runs through it, the bend is the rotation carrying the tip plane's measured rest onto its aim, and aiming both the same way straightens the run between them. The rates' one guarantee is kept by construction — the rotation at the base cut is identity however far apart the planes are aimed — along with the span's own length and the far part carried rigidly; what is given up is a bend that starts straight and tightens. `bend-span/2`; a v1 file is refused by name. Everything the tool got right is kept: the two named references and the geometry/bone pair said three ways, the **measured** out-of-plane figure, every “after” re-measured over the warped mesh, the traced reading with its `residual` and orange warning, the lit slab, the sha-bound export, and the per-joint table in chain order. | `npm run bend` 185 checks (the planes, the warp, no kink at the base at any aim, the joint table, the file and its refusals, and a body built with its neck 35° off its trunk straightened and re-measured over the warped mesh). `node tools/bend-browser.mjs` on Askeptosaurus: the panel names the body and warns that the shipped rest already carries its builder's correction and points at *Original pose (no rig)*; a handle found by hover, dragged and undone; the neck span read against three defensible trunk chords **29.1° apart**; a per-joint table `chest, neck_00…neck_03, skull` whose local steps sum to the turn (80.40° against 80.297°), which is the shape `carry_rest` consumes; the export hashed in the page equal to `sha256sum` on disk, accepted by `npm run triassic:bend` and refused stale and refused as `bend-span/1`. **The headline, on the original pose** (`askeptosaurus.origpose.glb`), span (0.101, −0.007, 0.263)→(0.347, −0.082, 0.422): at the default window and reach both traces wander and the panel says so; at a 26 % window and 2 % reach the residuals are 0.035/0.047, the two planes seat **41.40° apart** and the geometry reading says the same 41.4°; *Straighten it* leaves them 0.00° apart on a turn of 41.40° and the run between them re-measures **1.05° in the plane over the warped mesh**. Renders and numbers in `docs/triassic/verification/askeptosaurus-bend-*`; `docs/viewer-bend.md` and the CLAUDE.md bullet rewritten. `npm run bend`, `mouth`, `mark`, `stretch`, `sculpt`, `ancientseas`, `triassic` (625 checks), `eras`, `debug`, `npx tsc --noEmit` and `npm run build` pass; `mouth-browser`, `stretch-browser`, `mark-browser` and `debug-index-smoke` re-run green after the shared scene change. |

| T3D-31 | **partial** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-a2acf53ae29c58ecc` | Close a cut mouth with **the cut's own rim** instead of shells placed in the lumen, and ask first whether the cut is earning its place at all. Three kinds of generation: **shut** (the cut makes the aperture; cap it — `T.cap_mouth`), **a slit or shallow cavity** (judge per body), **gaping** (the aperture and its walls already exist; do not cut — `T.jaw_field_uncut`). `T.cut_rim` measures which kind a body is. | **Three bodies ported and delivered**, one per case, each with the same instruments before and after: **Rhaeticosaurus** (shut, plane cut — capped and domed; as drawn `Heavy` 4,626 px through → 0, 4,427–6,066 opened → 0; skin 2.81x unchanged), **Nothosaurus** (shut with a modelled slit and a *fitted tilted* cut — 225 boundary edges per half, 62 the cross-section at the skull joint and 163 in six closed curves, all capped; as drawn 2,790/2,509/2,044/1,849 opened → 0; skin 2.98x unchanged) and **Mosasaurus** (gaping — no cut at all; as drawn 71/292/0/222 px through → 0, the owner's holes at the corner of the mouth; skin 2.54x, exactly the cut body's, at a band swept 0.20→0.50). All three: exact `--package --decode` parity, LOD1 byte-identical to the twin, `lag.mjs`, `idle-bones.mjs`, `oral-shell-audit.mjs`, `throat-audit.mjs`, `hidden-parts.mjs --check`, portraits and asset sizes. `tsc`, `npm run build` and `npm run triassic` pass. **Also delivered: the instrument.** `gape-solid.py --as-drawn` hides what the runtime hides, because the plain run was proving a body the game does not draw; `tools/triassic/mouth-space.py` photographs whether a mouth reads as a mouth. **Not done: the other 24 bodies.** The roster-wide as-drawn sweep is `throat-repairs/as-drawn-sweep.txt` and the table in `oral-verdicts.md`; only Helicoprion and the three ported here read 0 opened, eight bodies show more than 20 px seen through the head (Atopodentatus 3,738, Hybodus 1,346, Henodus 407, Macrocnemus 343, Tanystropheus 142, Saurichthys 123, Dinocephalosaurus 120, Placodus 23). That is the rollout, and it is T3D-32. |

| T3D-32 | **split** | see T3D-32A..D | Roll T3D-31's mouth construction out across the remaining 24 bodies, one at a time, deciding each body's kind with `T.cut_rim` first: cap the cut where the generation arrived shut, drop the cut where it arrived gaping, and leave a body that needs nothing alone. Dinocephalosaurus is a small one to start with — its lining verdict stands and all it needs is `T.cap_cut` over the hinge cross-section its hidden plug is covering. | Per body: `gape-solid.py --as-drawn` at the measured peak of every opening clip, 0 through and 0 opened against the sweep's own before figure; `lag.mjs`, `skin-tears.mjs`, `idle-bones.mjs`, `oral-shell-audit.mjs`, `hidden-parts.mjs --check`; exact `--package --decode` parity with LOD1 byte-identical to the twin; a `mouth-space.py` sheet under `docs/triassic/verification/`; portraits and asset sizes. |
| T3D-32A | **active** | `session 01FLc4qT537AmBgjJqMfpt8y`, agent A | T3D-32 for the two worst bodies in the as-drawn sweep, plus T3D-23's Atopodentatus clause on the same builder so one owner holds it: **Atopodentatus** (3,738 px seen through the head as drawn) and **Hybodus** (1,346). Decide each body's kind with `T.cut_rim` before touching geometry. Atopodentatus also carries T3D-23's `Heavy`, which pulls the head 33.8 % of L back before reaching 7.1 % forward — decide or re-author, and mind that its `Heavy` is why that body's jaw junction runs at a 0.6 throat share. Hybodus also carries the `np.interp` hinge-plug lesson: its plug "fitted" at +0.004 clearance while standing 1.29 units clear of the nose with 126 of 207 vertices outside the animal, so anything that survives here is seated by ray parity against the closed intake, never by a table. | T3D-32's acceptance per body, and the Atopodentatus `Heavy` item recorded or re-authored with the same measurement re-run. |
| T3D-32B | **finished** | `session 01FLc4qT537AmBgjJqMfpt8y`, worktree `worktree-agent-aee0d06eb8df9ecf3` | T3D-32 for **Henodus** and **Macrocnemus**, with T3D-19's fringe clause on the one and T3D-20's cut clause on the other. | **Both delivered, both 0 through and 0 opened as drawn at the measured peak of *every* opening clip** rather than the three the sweep took. **Macrocnemus** — arrived shut (`T.mouth_cavity` nine vertices on the whole head; `T.cut_rim` one closed loop per half, 110 authored / 76 twin, hinge to within 0.002 of the snout, no boundary elsewhere), so `T.cap_cut` at the hinge (45/30 faces) then `T.cap_mouth` over the lip (67/48 rim edges, one cycle, 585/414 faces, domed 0.0105 raw off the measured section). Lining, hinge plug and rim flange all gone; the tooth rows stay, because teeth are the animal. As drawn, twelve clips: `Ability` 934 / `Snatch` 244 / `Bite` 343 / `Death` 577 through and 1,241–2,594 opened → **0 / 0 on all twelve**. The plain run is the warning: it read 0–2 through and 21–151 opened *before*, a body that passed while the game showed a hole. Skin **3.41x → 3.18x**. **T3D-20's cut, answered by naming the feature.** `pigment_seam` is a step detector that pools both flanks and maximises one pale-to-dark step; this generation's flanks are not painted alike (left pale block to 0.35 of the section, right to 0.20), so the pooled step sits between them **on the right flank's countershading boundary**, and behind the hinge it climbs to the flank boundary above the eye (0.660, 0.512). The median of those twelve is what put the shipped cut **0.03043 raw, 3.11 % of L**, off its own readings at x 0.3956 — reproduced exactly by this build. What the albedo actually carries, measured by binning the luminance up each flank: a sharp dark minimum at **0.375 of the section at every station of the snout**, pale 0.40–0.49 below it and mid 0.19–0.25 above — a painted lip. So the lip is read with `T.painted_line`'s matched filter on both flanks and the cut is a least-squares line fitted through those twenty readings (robust refit, pitch 10.28°, 20/20 kept): **0.00398 raw worst, 0.00167 rms, 0.41 % of L**, against the 0.005 bar; the median cut reads 0.00739 against the same line. **Henodus** — the cut makes almost all of the aperture (a modelled *slit*, 78 vertices, half height 0.0085–0.0124 on a head 0.07 deep; `T.cut_rim` one closed loop per half, 98/72, no boundary elsewhere), so capped. Two things this rim needed that T3D-31's two did not: the cut is **not a graph over any plane** (`cut_height` descends up to 0.038 outboard of the beak's inner width so T3D-02c's fringe stays on the skull), so each half is sheared onto the cut, capped, and sheared back — this builder's own trick for taking the cut; and the span is a **fan to a sunk hub** rather than `T.cap_mouth`'s planar triangulation, because the split also returns fringe pieces to the skull along an edge path that is not the cut and on the coarse twin that left a correct-sized 43 faces for a 45-gon with 5 edges still on the boundary — *the polygon was wrong, not the fill*. A fan closes any closed curve by construction; it is `T.cap_cut`'s own construction, already shipping at this body's hinge. The hub is sunk by `cap_mouth`'s measured rule (CAP_DOME of its own distance from the nearest rim vertex, bounded by the measured section): 0.00541 authored, 0.00471 twin. As drawn, eight clips: 407 / 404 / 242 / 176 / 150 / 91 / 76 / 69 through and 455/462/302/… opened → **0 / 0 on all eight**. Skin **4.81x, unchanged**. **T3D-19's fringe clause: the reading was wrong and a ray trace says so.** Casting from `gape-solid.py`'s own camera through every marked pixel and asking every surface on the line: `Bite`@0.200 plain **all 116**, `Bite`@0.200 as drawn **all 455**, `Ability`@0.100 plain **all 48** — *one back face of the mandible and nothing else*, on every single pixel. Not the fringe, not a tooth edge, not a one-polygon rim: a ray that entered through the aperture the cut made, with no oral geometry anywhere on its line. Exactly the class of hole a cap closes, and 0 now. Both bodies: `lag.mjs` 0 rim points open past 0.2 % (worst 0.00 % Henodus, 0.01 % Macrocnemus), `idle-bones.mjs`, `oral-shell-audit.mjs` ("no oral lining on authored, puppet or LOD" — **nothing hidden in play on either body any more**), `hidden-parts.mjs --check`, `mouth-space.py` sheets in `docs/triassic/verification/`, portraits re-rendered, `update-asset-sizes.mjs`, `tsc`, `npm run build` and `npm run triassic` all pass. **One thing does not, and it is not these bodies':** `macrocnemus/audit.mjs --package --decode` packages and verifies correctly and then stops on `assert.equal(clips, 26)` against 27, because the `Peer` shore gait is applied after the build by `tools/creatures/motion/apply.mjs` and that audit's clip count does not know about it. Straight off `build.py`, before the gait, it passes end to end with exact parity and LOD1 byte-identical to the twin; `Peer` was then re-applied as its README's reproduction says. Henodus audits clean with the gait question not arising. Recorded, not worked around — a central fix is in hand. **T3D-23's `hind_foot_L` (0.88 of its joint) is recorded rather than repaired, because the instrument does not see it:** `lag.mjs --verbose` measures exactly that quantity and reads `fore_foot_L` 1.03/1.00, `fore_foot_R` 1.05/1.00, `hind_foot_L` 1.06/**1.00**, `hind_foot_R` 1.04/1.00 — identical on the shipped file and on this rebuild, all four feet following their own joints exactly. Whatever produced 0.88 is not `lag.mjs` and the row does not name it. **And one finding nobody was looking for:** `macrocnemus.lod1.glb` dropped 1,496,524 → 917,048 bytes on a mesh whose triangles barely moved (8,574 → 8,458), because the hinge plug wore the body's pigmentation material (the right rule for an authored patch) and was exported *with the twin group* — so the vertex-coloured twin was carrying 578 KB of 2048² albedo and normal JPEG for an 86-vertex ellipsoid. Worth checking on the other bodies whose plugs wear the skin. |
| T3D-32C | **active** | `session 01FLc4qT537AmBgjJqMfpt8y`, agent C | T3D-32 for **Tanystropheus** (142 px), **Saurichthys** (123) and **Dinocephalosaurus** (120), with the older rows that sit on those same three builders. Dinocephalosaurus is the cheapest body on the roster to start with — its lining verdict stands and all it needs is `T.cap_cut` over the hinge cross-section its hidden plug is covering. Tanystropheus carries **T3D-20**'s cut (0.0116 raw, 0.9 %) and, with Dinocephalosaurus, **T3D-23**'s neck question: both animals' snaps are lateral sweeps of a neck already straight at rest (42 % lateral, 32–47 % back, ≤ 1 % forward) — decide whether a sideways sweep is the strike or a recoil, and record the decision either way. Saurichthys carries **T3D-21**'s right pectoral: `pec_tip_R` is 3.58 % of L from the nearest skin vertex (`pec_tip_L` 1.67 %), `pec_mid_R` 2.32 % (L 1.11 %), and the right chain dominates 126 vertices to the left's 247 — re-measure both chains with `K.measure_radii` and seat the joints on the fin, as Helicoprion's half of that row already did. | T3D-32's acceptance per body; Tanystropheus' cut ≤ 0.005 raw; the neck decision recorded; Saurichthys' pectoral chains within 20 % of each other in owned area and joint-to-skin distance. |
| T3D-32D | **active** | `session 01FLc4qT537AmBgjJqMfpt8y`, agent D | The remainder of **T3D-23** on three bodies no other batch claims, so the attack rule is finished off while T3D-32 runs: **Aphaneramma** `Heavy` −18.0 % / +11.5 % and `Ability` −24.9 % / +9.8 % of L on `anchor_attack_primary`; **Rhaeticosaurus** `Heavy` −14.4 % and never more than 1.0 % forward; **Askeptosaurus**' `TailWhip` moves the skull anchor 0.9 % while the tail is the weapon and the body has no tail anchor. Phragmoteuthis' dart (already delivered) is the worked example of what a re-authored attack looks like and what to measure. Rhaeticosaurus is T3D-31's own capped body, so its mouth is finished — do not re-cut it; check the as-drawn figure still reads 0 after any clip change. Askeptosaurus is the promoted posed generation (T3D-25) whose `posedExtentOverBind` is asserted per clip because the renderer sizes a body by its bind box. | Per item a recorded decision or a re-authored clip/anchor with the same measurement re-run, no body's other figures worse, and the full per-body instrument set green. |

| T3D-33 | **delivered on `worktree-agent-a7505d104913ef8c5`, merge held** | `session 01FLc4qT537AmBgjJqMfpt8y`, agent E | **Do not merge this branch before T3D-32A/B/C land.** It is finished and green on its own terms, and its new guard (`tools/triassic/clip-contract.mjs --check`, wired into `npm run triassic`) correctly fails on six files — `hybodus`/`saurichthys`/`macrocnemus` `.puppet` and `.lod1` — because those three bodies are genuinely still wrong and are being rebuilt by T3D-32A/B/C right now. Merging first would leave `main` red on that step for every other session until they arrive. Re-running `apply.mjs <id>` after each of those rebuilds lands the gait on all three variants and clears it. **A third fault, found by T3D-32B and not yet fixed on that branch**: a per-body `audit.mjs` bakes its own expected clip count, and that constant does not know about a post-build shore gait. Macrocnemus rebuilt clean and audited end to end *before* its `Peer` was re-applied, then stopped at `audit.mjs:76`, `assert.equal(clips.length, CLIPS)` — 27 against 26. That is neither the root assertion nor a family parity failure; it is a third place the gait is invisible to the body that carries it. Whoever finishes T3D-33 should fold it in, since a body can now pass the root check and the family check and still be unauditable. Note also that T3D-32B re-applied `Peer` with the *unfixed* `apply.mjs`, so Macrocnemus still carries its gait on the authored file alone and still needs a re-apply once T3D-33 lands. Original brief follows. | **A shore gait lands on the authored body alone, and takes that body's audit out with it.** Five Triassic bodies carry a gait clip added by `tools/creatures/motion/gaits.mjs` — Birgeria, Helicoprion, Hybodus and Saurichthys a `Flop`, Macrocnemus a `Peer` — and **none of the five has been auditable since**: `audit.mjs --decode` stops at `_pipeline/paired-audit.mjs:99` before it measures anything. Two separate faults, both measured. (1) The gait's `root` channels are **constant** — maxdrift exactly 0 over 25 frames on Birgeria and 121 on Macrocnemus, rotation held at the rest quaternion, translation at zero — so the root does not move and `rig.mjs:199` is honoured; the assertion tests that the channel does not *exist*, which is not what its message (`no root motion`) says, and is the roster's own recurring lesson about a measurement that misdescribes itself. (2) The gait is written to the **authored file only**: Birgeria's authored body carries 24 clips and its twin and LOD1 (byte-identical to each other) carry 23 with no `Flop` at all, so authored/twin clip parity — the pairing that *is* the pipeline's verification step — is broken by the tool, and would fail immediately after the root check even if that were relaxed. The era JSON meanwhile declares all 24 clips including `Flop` as looping, so the manifest promises the reduced model a clip it does not contain. Consequence on the record: Birgeria is the one body T3D-31's as-drawn sweep could never measure, and every ledger row citing `audit.mjs --package --decode` parity on these five took that evidence *before* the gait was applied. | The gait tools write the clip to the whole family (authored, twin, LOD1) and emit no constant root channel; the assertion tests what its message says; all five bodies audit clean; Birgeria measured into the as-drawn sweep at last; a guard in `npm run triassic` so a shipped Triassic body carrying a root channel, or a variant missing a clip its era JSON declares, fails loudly. Hybodus, Macrocnemus and Saurichthys are being rebuilt by T3D-32A/B/C right now — fix the tooling and the two unclaimed bodies (Birgeria, Helicoprion), and the other three are re-run as they land. |

## Finished baseline

- Twenty-seven Triassic authored bodies, procedural puppets and LODs are shipped and structurally
  validated. Every roster animal now has a finished body; Askeptosaurus also has an animated
  viewer-selectable backup model preserving its former generated surface.
- Archelon and Mosasaurus are shipped as standing visitors.
- The shared palate/floor oral-geometry architecture and first skinning-repair batch are on `main`.
  Current builders are being moved to its measured-room interface in small claimed batches so
  later skinning and animation repairs remain intact.
- Optional regenerations for Aphaneramma, Helicoprion, Mixosaurus, Mosasaurus,
  Phragmoteuthis and Rhaeticosaurus are deferred. Record defects discovered in those bodies, but do
  not spend Tripo credits on replacements in this pass.
- The eleven builders on the shared `T.lining` path have completed measured-room ports, rebuilt
  assets, paired audits and direct packaged oral-shell topology checks in T3D-08A through T3D-08F.
  Coelophysis, Macrocnemus and Tanystropheus use a distinct ShoreKit oral implementation; their
  conversion is tracked separately in T3D-09.

## Coordination rule

Before starting a newly discovered repair, add a row with state **active**, a single owner and a
bounded completion test, then commit and push that claim. If a task cannot proceed, change its
state to **blocked** and name the missing input. Never leave completed work marked active.

## Current evidence

[`roster-finish-matrix.json`](roster-finish-matrix.json) is generated from the packaged GLBs;
[`roster-finish-audit.md`](roster-finish-audit.md) explains its checks and records the scoped
follow-ups. The matrix currently passes the complete packaged-body contract for all 27 bodies.
Shonisaurus' `Grab`, `Breath` and shared paired audit are complete; Henodus' `Grab` now loops.
The throat audit and priority repairs are finished in T3D-02; of the six custom oral-builder
recoveries in T3D-12, the three in T3D-12B (Dinocephalosaurus, Keichousaurus, Phragmoteuthis) are
delivered with their verdicts in `throat-repairs/oral-verdicts.md`, and T3D-12A's three remain. A
structural contract pass is not a whole-roster visual approval.

The post-integration `npm run triassic` reaches its gameplay assertions, but current `main`'s new
shore-post test does not spawn a Coelophysis among its locally materialized posts for seed
`5052026` (it sees Mystriosuchus and Tanystropheus). That assertion arrived with the concurrent
shore merge and is independent of these prop GLBs. It remains a small non-art follow-up rather
than being hidden as a successful roster gate.
