# Checkpoint — 20 September 2026, paused after the verification pass

> For current ownership and completion state, use [`3d-work-status.md`](3d-work-status.md). Do not
> restart a row claimed there. This file is the narrative of where the session stopped.

Everything below is on `main` unless it names a `worktree-agent-*` branch; every such branch is
pushed to `origin`. The working branch is `claude/3d-modelling-animation-review-fvrvz3` and carries
nothing `main` lacks.

## Where the roster stands

**All 27 Triassic bodies are shipped and playable**, plus Archelon and Mosasaurus as standing
visitors. Every one is on its final oral contract — a separate palate and floor, or nothing at all
(`throat-repairs/oral-verdicts.md` is the per-animal verdict) — and every jaw cut is closed roster-
wide (`jaw-skinning.md`). The seven Tripo props are drawn in the game and catalogued in the viewer.
`npm run triassic` (535 checks, now including `lag.mjs` and `hidden-parts.mjs`), `npm run props`,
`npm run sim:gate`, typecheck and build all pass on `main`.

## Landed this pass (rows T3D-12 through T3D-17, T3D-21/23 in part)

- **Mouths**: the six custom builders ported (Hybodus, Saurichthys, Placodus, Keichousaurus; Dinocephalosaurus and
  Phragmoteuthis carry nothing). Shonisaurus' eyes moved forward and up; Nothosaurus' head turned
  from −20° yaw to −0.3° in the mesh with the cut refitted to the lip.
- **Jaws**: `T.jaw_junction` — one weight field over body and mandible shell — closed cut openings of
  up to 6 % of a body on all 19 shelled bodies; no skin figure worse.
- **Fins and attacks**: Helicoprion's pectoral roots re-seated (14.33× → 4.21×); Phragmoteuthis'
  attacks re-authored as a forward dart (reach 3–4 % → 14 % of L).
- **Viewer**: the mouth editor (`&mode=mouth`), a *Base pose* button in the Animations pane, and mark
  mode hashing the file on stage.
- **Props**: the renderer's `floraSets` never had the seven new kinds — placed, collided against,
  never drawn. Fixed; the table is total over `FloraKind` now.

## The last pass: one row landed, two were stopped for tokens

**T3D-18 and T3D-22 are finished and on `main`.** Cartorhynchus' fore blades are seated as a pair
(rigid residual 2.36 / 2.46 %, 4 % apart; skin 3.72x -> 2.98x) and its "mouth corner leak" turned out
to have been **already closed by T3D-15** — T3D-17 measured the pre-rebuild file; a ray through the
failing pixels found no front face and the far cheek's inside, which is exactly the slot the jaw
junction closed. The shipped file reads 0 px on all six opening clips. Placodus' `Sprint` now
paddles (35 -> 155 degrees swept per cycle at every limb root) over an unchanged tail wave, while
`Swim` keeps the research's slow tail scull; its `Eat` pixels are traced to the rim flange and the
front cap seen edge-on (silhouette, recorded not closed) and its worst skin figure is unchanged at
12.36x, though `Sprint` itself went 9.42x -> 11.33x — the cost of the root's arc, recorded.

One thing that row did **not** meet: Cartorhynchus' trunk-blend symmetry target (12 % apart against
a 10 % bar; the right base genuinely sits 0.024 L further forward, which is the pose). The agent
judged further symmetrisation would pull a chain off its own blade's base. Read it as a partial.

**Two agents were stopped mid-work with nothing committed** and their rows are back to **pending,
unclaimed** on the ledger. Their worktree branches hold no commits; the briefs are reproducible
from the row text.

| Row | Bodies | State |
|---|---|---|
| T3D-19 (minus Placodus, done) | Cymbospondylus, Henodus, Shonisaurus — ray-trace the residual gape pixels | not started |
| T3D-20 | Coelophysis, Macrocnemus, Tanystropheus — fit the mouth cut to the lip instead of a median | got as far as writing a lip instrument into `shorekit.py`; nothing kept |
| T3D-21 (Saurichthys half) | Saurichthys' right pectoral chain seated off its fin | not started |
| T3D-23 (remainder) | Atopodentatus/Aphaneramma/Rhaeticosaurus `Heavy`; Dinocephalosaurus/Tanystropheus neck sweeps; Askeptosaurus' tail anchor | not started |

## What remains after those land

Five decisions that all resolve to a Tripo regeneration, which the ledger keeps out of scope until a
human rules — recorded with their numbers in the task list and `preview-mesh-defects.md`:

1. **Helicoprion** — the mouth shuts in profile but not head-on (the gape is a notch cut into the
   front of the head; no mandibular flank exists to raise) and the whorl's "holes" are painted, not
   modelled. A mouth-closed regeneration fixes both.
2. **Phragmoteuthis** — twelve arms where a decabrachian has ten; the geometry cannot say which two
   are extra.
3. **Aphaneramma** — the tail crosses its own station (a closed ring in the raw generation); no rig
   can take it out.
4. **Mosasaurus** — built from a gaping generation; 13 % of the mandible pushes through the head's
   own section at the shut pose (0.72 % of a body length; one pink sliver at the corner in Idle).
5. Placodus' skin figure (12.36×, `Pry` on `fore_paddle_L`) is the one outstanding skinning repair.

And one thing nobody has looked at by eye: T3D-15's twelve after-renders it did not inspect, and
the game itself with the shore animals on. The instruments are green; a human pass through the
viewer is the review this pass explicitly did not wait for.
