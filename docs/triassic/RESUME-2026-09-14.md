# Checkpoint — 14 September 2026

Written so this session can be picked up cold. Everything described here is on `main`; the
working branch is `claude/3d-modelling-animation-review-fvrvz3` and carries nothing `main` lacks.

## Where the bodies stand

| State | Animals |
|---|---|
| **Shipped** (`tools/triassic/shipped.json`) | Nothosaurus, Shonisaurus |
| **Built, awaiting review** (`src/content/triassic/review-bodies.json`) | Placodus, Helicoprion, Dinocephalosaurus |
| **Being built right now**, in agent worktrees | Tanystropheus, Macrocnemus, Coelophysis · Cymbospondylus, Mixosaurus, Hupehsuchus, Cartorhynchus · Birgeria, Saurichthys, Hybodus, Askeptosaurus |
| **Not started** | Henodus, Keichousaurus, Odontochelys, Atopodentatus, Ceratites, Phragmoteuthis, Rhaeticosaurus, Aphaneramma, Mystriosuchus |
| **Off-roster** (`src/content/triassic/expansion.json`) | Archelon (generation landed) · Mosasaurus (blocked, see below) |

24 preview bodies are published. Every roster subject now has either a generation or a body.

## Work in flight

Four background agents, each in its own git worktree under `.claude/worktrees/`:

| Agent | Doing |
|---|---|
| `a5989436daf7da12f` | the three shore animals |
| `a693912c13b5208a2` | Cymbospondylus, Mixosaurus, Hupehsuchus, Cartorhynchus |
| `ae393a70ae7a52c10` | Birgeria, Saurichthys, Hybodus, Askeptosaurus |
| `a59a21326bd8ae236` | filling Helicoprion's tooth whorl and Nothosaurus' webbed feet |

Each has been told, mid-flight, to: record `meanCurvatureRadiusOverSection` per body region and
paired-limb asymmetry (for the neutral-pose pass); keep swimming primary and merge each creature
to `main` as it finishes rather than per batch; meet the mouth/dash/anchor bar below; and close the
jaw outside the attack clips where a generation arrived gaping.

**They are launched but not spawned-again-safe.** Do not start a duplicate for any animal in the
table above. Disk is the binding constraint on the next wave: 5.5 GB free with four worktrees out
at 1.7–2.8 GB each, so the remaining nine start as worktrees are reclaimed.

## Standing rules established this session

All are in `CLAUDE.md` unless noted.

- **Tripo is fed one view of one animal, never a contact sheet.** A six-panel sheet fed whole came
  back as six turtles in one GLB. The four-view turnaround is a human review artefact.
- **A playable Triassic animal swims.** Generations posed for land still get the swim set as their
  locomotion; a walk is an extra. Shore animals are the exception, being unplayable.
- **A creature merges to `main` when it is finished, not when its batch is.**
- **A mouth must read as a mouth, not a hole**: one closed skinned lining wound inwards, skin
  double-sided behind it; the jaw seam fitted to the model's own lip contour; three anchors with
  the attack anchor on the bone that delivers the blow. Proving it needs a cull-shim comparison —
  comparing against the plain background measures the backdrop and always passes.
- **A limbed swimmer's dash has to paddle**, swept angle per limb root recorded as a number.
- **A subject whose era is undecided stays off the roster**, because roster membership is what puts
  an animal in the sea.
- **What may be authored on a Tripo body is decided by how simple the shape is**, and must wear the
  creature's own texture.
- Which pipeline steps are judgement and which are mechanical: `docs/triassic/04-tripo-pipeline.md`.

## Open tasks

- **#23** the nine unstarted bodies.
- **#24** give every body a `Neutral` pose and re-base its clips on it — a clip, never a re-bind,
  jaw closed, validated on the authored body; the curvature ratio decides rig vs mesh straightening.
- **#25** Archelon and Mosasaurus as off-roster bodies.
- **#26** the full mouth / dash / attack / anchor audit, to run once every creature is on `main`.

## Blocked, and on what

- **Mosasaurus** needs a re-rendered reference: one animal, single three-quarter view, **mouth
  closed**, no panels or labels. `intake/mosasaurus-3d.jpeg` is still in `intake/`, which is what an
  unintegrated handoff is supposed to look like.
- **Nothosaurus neck redraw** and **Shonisaurus dorsal fin + crescent fluke** are open image
  requests.
- The **Mesozoic scope decision** (`05-mesozoic-expansion.md`): widen the Triassic, or a fourth
  game. Nothing is being built that pre-empts it.

## Environment notes worth not rediscovering

- Blender 5.2.1 at `/opt/blender/blender`. **EEVEE and Workbench need EGL, which this container does
  not have**, so `tools/triassic/tripo/review.py` fails at its render step. Its `audit` and
  `export_preview` are fine: import the module and call them directly rather than editing the tool
  (there is a working example of this in the Archelon intake).
- Raw Tripo bodies are unstitched patch soup. Weld at 0.0005 before any connected-component or
  Laplacian work, or you measure the triangulation instead of the animal.
- The Tripo v3 API has **no task-listing endpoint**; `/v3/tasks/{uuid}` is all there is, so a task
  id that was not written down is gone. A generation costs 30 credits.
- The API key used this session is in the session transcript and **should be rotated**.
