# Pause checkpoint — 13 September 2026

The latest user instruction pauses complex work. Only Tripo generation, routine Blender
processing and main-branch delivery may continue on a simpler agent. Do not begin new image
generation, rigs, procedural puppets, animation or anatomical refinement until the user resumes.

## Delivered before the pause

- Main `237bf24`: four candidate03 canonical redraws (Aphaneramma, Mystriosuchus,
  Rhaeticosaurus, Tanystropheus), plus approved modeling inputs for Cymbospondylus,
  Odontochelys and Macrocnemus. Reference sources and prompts are preserved.
- Main `8914a81`: complete Ancient Seas artwork, 25 creature reference boards,
  nine regional boards and two scale plates. Trilogy artwork has 29 runtime files,
  genuine transparency where required, and all files are below 600 KB.
- Main `926bc81`: Shonisaurus lip/texture correction. Original albedo restored,
  mouth stays closed outside feeding; zero sampled lip inversions. Skeleton/clips
  preserved, puppet/LOD unchanged. Portraits and audits refreshed. The model remains
  a preview; source tooth/rim detail and reconstructed mouth tissue have limitations.
- Validation: 260 Ancient Seas checks, 370 Triassic checks, typecheck and build pass.
  Chrome title-page and live single-sided Shonisaurus Idle/Heavy reviewed.

## Routine work allowed during pause

The image review window ends **2026-09-13 15:20:54 UTC**. After that, sync main and
rebuild the plan with `tools/triassic/tripo/batch-plan.ts`. Its hashes must match the
current selected canonical and modeling input. There were 19 ready subjects at the pause,
17 playable and two ambient. API wallet last verified: 655 credits; prior jobs cost 30 each.
Never buy credits or silently retry paid failures. Newly approved drawings without verified
modeling inputs remain blocked during this pause.

`run-batch.mjs` generates/resumes into `local/triassic-authoring/<id>/tripo/`.
`process-batch.mjs --process` preserves original GLBs, sanitized provenance, static previews
and Blender audit renders under `tools/triassic/creatures/<id>/`. Commit and push these
source deliverables to main. Do not add unrigged sources to `shipped.json` or clear badges.
Update `PRODUCTION-SESSION.json` with actual completions, failures and wallet use.

## When the user resumes

1. Confirm all eligible raw bodies are preserved, processed and committed.
2. Review each new body anatomically, then create its volume-matched procedural puppet.
3. One exact skeleton, inverse binds, anchors and action samples per authored/puppet pair;
   build real mouth interiors and movable jaws where appropriate. Use Astra for these
   creative and difficult decisions, a simpler agent for routine exports and packaging.
4. Finish creatures one by one with dynamic species-specific movement, deformation and
   single-sided visual QA, portraits, registration and main commits. Remove the creature's
   preview warning only when its pipeline is actually complete; do not invent human approval.

Working checkout: `cambrian/local/expansion-repo` (`devonian-assets`). Publish checkout:
`cambrian/local/devonian-publish` (`main`). Viewer runs on port 5180. Use Blender 5.2 at
`/Applications/Blender.app/Contents/MacOS/Blender`; background jobs require normal desktop
permissions for Metal. Keep old untracked intake files, local intermediates and `.blend1`
backups out of commits. No credentials belong in the repository.

## Routine raw-model batch completed during the pause

The post-window plan contained 19 eligible subjects: 17 playable and two ambient. All 19
tasks succeeded and all artifacts were downloaded. Eighteen were new paid submissions;
Cymbospondylus resumed its existing successful task after a transient artifact-download
failure. Its completed task was refreshed and downloaded with idempotent GET requests, so
there was no second paid submission.

Each successful task record reports 30 credits consumed, for 570 credits total. The wallet
was verified at 655 before the run. Its arithmetic remainder is 85; billing UI was not
independently rechecked after the run, so 85 must not be described as a verified balance.
The temporary credential file was removed after the batch and no credential, upload token or
signed download URL is stored in the repository.

Static processing preserved the exact raw GLB, sanitized task metadata, a preview GLB, a
Blender audit and side/top/three-quarter review renders for each of:
Cymbospondylus, Dinocephalosaurus, Helicoprion, Atopodentatus, Askeptosaurus, Placodus,
Hybodus, Birgeria, Mixosaurus, Henodus, Saurichthys, Hupehsuchus, Keichousaurus,
Cartorhynchus, Odontochelys, Ceratites, Phragmoteuthis, Macrocnemus and Coelophysis.
Source and preview hashes match their metadata and audits. These are unrigged source bodies;
none was registered as shipped or had its preview warning cleared.

Visual review found correction work that remains paused. Askeptosaurus has a detached pale
geometry island and a severely curled tail. Dinocephalosaurus has a tightly curved, raised
neck. Henodus has a pronounced hard scuted carapace and a bifurcated-looking tail silhouette.
Keichousaurus has paddle-like limbs without clearly resolved digit-bearing feet. Hybodus has
a sharply raised tail pose. Preserve these raw outputs as provenance and route the issues
through anatomical review and authored-model correction after complex work resumes; do not
silently treat the static previews as approved production models.
